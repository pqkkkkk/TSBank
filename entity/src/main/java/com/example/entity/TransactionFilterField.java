package com.example.entity;

public enum TransactionFilterField {
    ID,
    TYPE,
    FROM_AMOUNT,
    TO_AMOUNT,
    ACCOUNT_ID,
    COUNTER_PARTY_ID,
    FROM_CREATED_DATE,
    TO_CREATED_DATE,
    FROM_CREATED_TIME,
    TO_CREATED_TIME
}
